---
description: graphify knowledge graph context
alwaysApply: true
---

This project uses Graphify for token-efficient codebase exploration. The local
knowledge graph lives at `graphify-out/` when generated.

Before using Read, Grep, Glob, or broad Bash searches to explore the codebase,
query Graphify first:

- `graphify query "<question>"` — scoped subgraph for any codebase or architecture question
- `graphify path "<A>" "<B>"` — dependency path between two symbols
- `graphify explain "<concept>"` — all nodes related to a concept

If `graphify-out/graph.json` is missing, build it with:

```bash
graphify extract . --code-only --no-viz
```

Use `--code-only` for normal engineering work so docs/media extraction does not
spend LLM tokens.

This applies to every subagent spawned for code exploration. Include this rule
explicitly in subagent prompts that need repository context.

Only use Read/Grep/Glob directly when:
1. graphify has already oriented you and you need to modify or debug specific lines
2. `graphify-out/graph.json` does not exist yet

- If `graphify-out/wiki/index.md` exists, navigate it instead of reading raw files
- Read `graphify-out/GRAPH_REPORT.md` only for broad architecture review when query/path/explain do not surface enough context
- After modifying code files, run `graphify update .` to keep the graph current (AST-only, no API cost)
